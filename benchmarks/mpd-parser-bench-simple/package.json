{
  "name": "mpd-parser-bench-simple",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:tizen": "VITE_BASE=./ vite build --base ./",
    "build:webos": "VITE_BASE=./ vite build --base ./",
    "package:tizen": "npm run build:tizen && node scripts/package-tizen.js",
    "package:webos": "npm run build:webos && node scripts/package-webos.js"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@svta/cml-utils": "^1.3.0",
    "@svta/cml-xml": "^1.1.2"
  }
}
